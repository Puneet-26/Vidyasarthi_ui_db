{
  "buildCommand": "git config --global --add safe.directory /vercel/path0/flutter && git clone https://github.com/flutter/flutter.git -b stable flutter && ./flutter/bin/flutter build web",
  "outputDirectory": "build/web"
}
